blobs 
trees
commits

Blobs = 

ssh-keygen -t eg25519 -C guilehrme@email.com

eval $(ssh-agent -s)


git init - git  add - git commit

git add * (toda a pasta será adicionada)

git commit -m "Commit Inicial" 

git config --global --unset user.email

git config --global --unset user.name
